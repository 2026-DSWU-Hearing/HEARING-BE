services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: hearing
      POSTGRES_PASSWORD: hearing
      POSTGRES_DB: hearing
    ports:
      - "5433:5432"
    volumes:
      - hearing_pg:/var/lib/postgresql/data

volumes:
  hearing_pg:
